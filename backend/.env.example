# Application
APP_NAME=RE:WORK API
DEBUG=false
DEMO_MODE=true

# Database (optional for Checkpoint 1 — fixtures work without DB)
DATABASE_URL=postgresql+psycopg://rework:rework@localhost:5432/rework

# LLM (not used in Checkpoint 1)
# GEMINI_API_KEY=
